name: Enforce Dependabot Config
on:
  push:
    branches: [main]
    paths: [".github/dependabot.yml"]
  pull_request:
    paths: [".github/dependabot.yml"]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  check:
    permissions:
      contents: read
    uses: nabhold/shared/.github/workflows/enforce-dependabot-config.yml@f8df9b90ffe8fef5cbf6d14a9bcf868f723d1464 # v1.1.0
